<!DOCTYPE html>
<html lang="no-nb">
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>{{title}}</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/github-markdown-css/2.8.0/github-markdown.min.css" rel="stylesheet" />
    <script
      src="https://code.jquery.com/jquery-3.2.1.min.js"
      integrity="sha256-hwg4gsxgFZhOsEEamdOYGBf13FyQuiTwlAQgxVSNgt4="
      crossorigin="anonymous">
    </script>
    <style>
      .body {
        position: absolute;
        margin: 0;
        padding: 0;
        left: 0;
        right: 0;
        top: 0;
        bottom: 0;
      }
      .container {
        height: 100%;
      }
      .content {
        display: flex;
        height: inherit;
      }
      .aside {
        width: 250px;
        min-width: 200px;
        background-color: #556270;
      }
      .hidden {
        display: none;
      }
      .visible {
        display: block;
      }
      .list {
        list-style-type: none;
        padding-left: 20px;
      }
      a {
        font-family: -apple-system,BlinkMacSystemFont,"Segoe UI",Helvetica,Arial,sans-serif,"Apple Color Emoji","Segoe UI Emoji","Segoe UI Symbol";
        font-size: 16px;
        color: #FFF;
        text-decoration: none;
      }
      a:hover {
        color: #D6D6D6;
      }
      a:visited {
        color: #FFF;
      }
      .markdown-body {
        /* from https://github.com/sindresorhus/github-markdown-css */
        box-sizing: border-box;
        min-width: 200px;
        max-width: 980px;
        margin: 0 auto;
        padding: 45px;
      }
    </style>
    <script type="application/javascript">
      $(document).ready(() => {
        $('a').on('click', (e) => {
          e.preventDefault();
          $('#markdown').children().each(function(index) {
            const selectedItemId = $(e.currentTarget).attr('href');
            if (this.id === selectedItemId) {
              $(this).addClass('visible').removeClass('hidden');
            } else {
              $(this).removeClass('visible').addClass('hidden');
            }
          });
        });
      });
    </script>
  </head>
  <body class="body">
      <div class="container">
        <div class="content">
          <aside class="aside">
            {{#list components}}
              <a href={{id}}>{{title}}</a>
            {{/list}}
          </aside>
          <main>
            <article class="markdown-body">
              {{#markdown components}}{{/markdown}}
            </article>
          </main>
        </div>
      </div>
  </body>
</html>
